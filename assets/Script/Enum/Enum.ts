export enum SFXType {
    Spawn = 0,
    Merge = 1,
    Wrong = 2,
}

export enum GameMode {
    CLASSIC,
    HARD,
    JOURNEY
}

export enum GameState {
    MainMenu,
    Gameplay,
    Pause,
    Victory,
    Defeat
}
