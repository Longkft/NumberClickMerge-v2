export enum SFXType {
    Spawn = 0,
    Merge = 1,
    Wrong = 2,
}

export enum GameMode {
    CLASSIC,
    HARD,
    MAP,
    JOURNEY
}

export enum GameState {
    MainMenu,
    map,
    Gameplay,
    Pause,
    Victory,
    Defeat
}
