export enum SFXType {
    Spawn = 0,
    Merge = 1,
    Wrong = 2,
}

export enum GameMode {
    CLASSIC,
    HARD
}

export enum GameState {
    MainMenu,
    Gameplay,
    Pause,
    Victory,
    Defeat
}
